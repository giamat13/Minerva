__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

.venv/
venv/
env/

.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

.idea/
.vscode/
.DS_Store

# Session-local task tracking, not project documentation.
TODO.md

# Local, machine-specific configuration (never commit endpoints/secrets).
minerva.local.toml
.env

# Training artefacts. The corpus is downloaded by `minerva prepare-data`, not
# vendored (see src/minerva/training/data.py), and checkpoints are large
# binaries that belong in a release, not in git history.
data/
checkpoints/
*.pt
*.bin

# Scratch downloads of CI artifacts, used to compare a hosted run against
# the local one (see CLAUDE.md section 8). Not source.
ci_artifacts/
ci_reports/

eval_root/
checkpoints/snapshot/
eval_combo/
